import { NextResponse } from "next/server";
export async function GET() { return NextResponse.json({ data: [{ id: "zapier", name: "Zapier", description: "Trigger workflows when clips are ready.", events: ["clip.completed", "project.completed"] }, { id: "slack", name: "Slack", description: "Send notifications to a channel.", events: ["clip.completed", "earning.created"] }, { id: "notion", name: "Notion", description: "Sync clip metadata to a database.", events: ["clip.created", "clip.completed"] }, { id: "webhook", name: "Custom webhook", description: "Deliver signed events to any HTTPS endpoint.", events: ["*"] }] }); }
