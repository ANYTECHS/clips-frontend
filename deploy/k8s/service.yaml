apiVersion: v1
kind: Service
metadata:
  name: clipcash-frontend
  namespace: clipcash
  labels:
    app.kubernetes.io/name: clipcash-frontend
spec:
  selector:
    app: clipcash-frontend
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
