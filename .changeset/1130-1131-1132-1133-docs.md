---
"clipsproject": patch
---

Documentation and Storybook improvements (#1130, #1131, #1132, #1133)

- Storybook: add stories for platform, skeleton, progress, offline banner, comparison player and virtual list components; add global session/auth/i18n providers and an App Router mock; fix broken existing stories (named vs default imports, `@storybook/test` → `storybook/test`, missing props, stories for deleted components); fix the Storybook 10 docs-blocks import, the cross-platform `staticDirs` path, and the deploy workflow's Node version and env; refresh STORYBOOK.md
- Architecture: add the system overview, diagrams, data flow, key design decisions, technology rationale and a maintenance policy to docs/ARCHITECTURE.md
- Contributor guide: rewrite CONTRIBUTING.md (onboarding, setup, review process, good first issues, communication) and add PR and issue templates
- Environment: add docs/ENVIRONMENT_VARIABLES.md and rewrite .env.example to cover every variable
